---
- name: Converge
  hosts: all
  tasks:
    - name: "Install base"
      include_role:
        name: base
    - name: "Install java"
      include_role:
        name: java
    - name: "Install adr"
      include_role:
        name: adr
