---
# version of node to install, defaults to 'latest'
node_node_version: 'node'

node_nvm_version: '0.39.4'

# Install path for nvm (defaults to $HOME/.nvm)
node_nvm_install_path: "{{ ansible_env.HOME }}/.nvm"
